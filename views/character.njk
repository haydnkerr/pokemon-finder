<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emoji Lister</title>
    <link href="./style.css" rel="stylesheet">
</head>

<body>
    <h1>Rick and Morty Characters</h1>
    <main>
            <h2>{{ character[num].name }}</h2>
            <img src="{{ character[num].image }}" alt="{{ character[num].name }}" title="{{ character.name }}">
    </main>
</body>
</html>